// 'use strict';

// const form = document.querySelector('.modal-window__form');

// const validateForm = (selector, rules) => {
//   new window.JustValidate(selector, {
//     rules: rules,
//     submitHandler: (form, values, ajax) => {
//       console.log(form);
//     }
//   });

  // let formData = new FormData(form);

  // fetch('mail.php', {
  //   method: 'POST',
  //   body: FormData
  // })
  // .then(data => {
  //   console.log(data);
  //   console.log('Отправлено');
  // })

  // form.reset();
  
// }

// validateForm('.modal-window__form', {fio: {required: true}, tel: {required: true}});